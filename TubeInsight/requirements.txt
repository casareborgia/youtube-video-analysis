# TubeInsight AI — 필수 의존성
# 설치: pip install -r requirements.txt

yt-dlp>=2024.10.7      # 유튜브 메타데이터·자막·댓글 수집
edge-tts>=6.1.0        # 무료 고품질 한국어 나레이션 TTS
numpy                  # 오디오 데이터 처리
soundfile              # 오디오 파일 읽기/쓰기
av                     # 오디오 포맷 변환 (녹음 파일 정규화)

# ── 선택 사항 (없어도 앱은 정상 동작합니다) ──────────────────
# '내 목소리 학습(보이스 클로닝)' 기능을 쓰려면 아래 2개를 추가 설치하세요. (용량 큼, 수 GB)
# torch
# qwen-tts
